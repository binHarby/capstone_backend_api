# Tables:
## Users:
	- id (auto generated)
	- email (input)
	- password(input)
	- Birthday(input)
	- bloodtype( input)
	- height (input)
	- weight (input)
	- cal_diff (input)
	- age (calculated and updated on creation and login)
	- main_cals (calculated utils->get_tdee + cal_diff)
	- BMI(calculated utils->get_bmi)
	- created_at (calculated on DB)
## desiseas:
	- name (PK)
	- rule (co-pk)
	- 'desc'
	API Logic (input: name -> return a_res)
	app logic (pass to usr_res all the a_res(s))
## usr_res:
	- user_id (FK (id) from user table, and PK)
	- desiseas_name (UK (name) from desiseas)
	- des_rule (UK from desiseases rule
## usr_state:
	- user_id (FK (id) from user table, and PK)
	- X_l (get maros,micros in place of X = limit)
	- X_c (get
